# ------------------------------------------------------------------- Adgang
# Uden disse to svarer serveren kun på kald fra samme maskine. Det er med
# vilje: så kan appen ikke udrulles offentligt uden adgangskontrol ved et uheld.
#
# Lav dem med:  npm run kodeord
AUTH_PASSWORD_HASH=""
SESSION_SECRET=""

# ---------------------------------------------------------------- AI-udbyder
# Bilagslæsning og revisor-chat. Uden en nøgle er begge dele slået fra, og
# appen siger det tydeligt i stedet for at gætte på et resultat.
#
# Gemini er standardvalget: den tager PDF direkte, også scannede sider, og
# læser fotograferede kvitteringer bedst. Det er den, appens kernefunktion
# står og falder med.
GEMINI_API_KEY=""

# DeepSeek som alternativ. Billigere, men den tager ikke PDF (teksten trækkes
# så ud på serveren først) og billeder tokeniseres til højst 384 tokens, hvilket
# er groft til en tætskrevet kontrakt på et foto.
DEEPSEEK_API_KEY=""

# Er begge nøgler sat, vælges Gemini. Sæt "gemini" eller "deepseek" for at
# tvinge valget.
AI_UDBYDER=""

# Modelnavne. Lad dem stå tomme for at bruge standarderne.
GEMINI_MODEL=""
DEEPSEEK_MODEL=""
DEEPSEEK_VISION_MODEL=""

# ----------------------------------------------------------------- Websøgning
# Revisor-chatten slår regler og satser op på skat.dk og retsinformation.dk i
# stedet for at svare efter hukommelsen. Uden nøgle bruges DuckDuckGo.
TAVILY_API_KEY=""

# -------------------------------------------------------------- Ruteberegning
# "Beregn afstand"-knappen ved kørselsfelterne. EU-baseret, gratis niveau
# (2.500 kald/dag), intet betalingskort. Opret en nøgle på openrouteservice.org.
# Uden nøglen virker appen uændret — man taster kilometer manuelt som hidtil.
OPENROUTESERVICE_API_KEY=""

# ------------------------------------------------------------- Google Drev
# Automatisk backup af bilag og et periodisk datasnapshot til brugerens egen
# Google Drev. Scope er kun drive.file — appen kan udelukkende se og skrive
# filer, den selv har oprettet. Uden disse tre virker appen uændret, bare
# uden Drev-backup.
#
# Opret et OAuth-klient-id i Google Cloud Console (API'er og tjenester →
# Legitimationsoplysninger → OAuth-klient-id, type "Webapplication").
# GOOGLE_REDIRECT_URI skal matche nøjagtigt det, der står registreret der,
# typisk https://<dit-domæne>/api/google/callback.
#
# VIGTIGT: så længe OAuth-samtykkeskærmen står i "Testing" i Google Cloud
# Console, udløber refresh-tokenet automatisk efter 7 dage, uanset hvor tit
# appen bruges — at tilføje sig selv som testbruger løser IKKE dette. Sæt
# samtykkeskærmen til "In production", før forbindelsen bruges til noget,
# det ville være et problem at miste. drive.file-scopet udløser en
# "Google har ikke verificeret denne app"-advarsel ved forbindelse; for en
# enkeltbrugerapp er løsningen at klikke sig igennem den (Avanceret → Gå til
# appen), ikke at gennemgå Googles fulde appverifikation.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI=""

# --------------------------------------------------------------------- Lager
# Hvor data og bilag gemmes. Uden DATABASE_URL bruges en JSON-fil og en
# bilagsmappe under denne sti.
DATA_DIR="./data"

# Postgres på Railway. Når den er sat, træder Postgres-driveren i stedet for
# fil-lageret. Lad den stå tom, indtil databasen findes.
DATABASE_URL=""

PORT="3000"
